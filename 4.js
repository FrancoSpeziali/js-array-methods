/*

    Assignment 4 - push()

    Update the function below:

    1. Create a FOR loop:

        - Each iteration of the loop should generate a random number

        - Generate 10 random numbers, each number must be between 0 - 100

        - You can use this code for the random number generation

        Math.random() * 100;

    2. For each random number generated, "push()" the number into the "values" array

    3. console.log() the "values" array

*/

function assignment() {

    const values = [];

}

assignment();
