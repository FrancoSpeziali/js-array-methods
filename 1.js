/*

    Assignment 1 - Array.isArray()

    Update the function below:

    1. Update the function to receive an argument "array"

    2. Using Array.isArray() check if the value "array" is an array or not

        - if it not an array, using console.log print "Value is not an array"

        - if it is an array, using console.log print "Value is an array"

    3. Test your function by providing it:

        - with an array

        - and a value which is not an array

*/

function assignment() {
}

assignment();
