/*

    Assignment 8 - push() and forEach()

    Update the function below:

    1. Update the function to receive an argument "words". This will be an array.

    2. Using the "forEach()" method

        - iterate through each item in the array "words"

        - "push()" each item returned by the "forEach()" method into the array "values"

    3. console.log() the "values" array

*/

function assignment() {

    const values = [ "fee", "fi" ];

}

assignment([ "fo", "fum" ]);
